apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chatter-helm.fullname" . }}-test-connection"
  labels:
    {{- include "chatter-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chatter-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
