apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatter-helm.fullname" . }}
  labels:
    {{- include "chatter-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "chatter-helm.selectorLabels" . | nindent 4 }}
