# MongoDB and Redis subcharts
redis:
  enabled: true
  architecture: standalone
  auth:
    enabled: false

# mongodb:
#   enabled: true
#   architecture: standalone
#   auth:
#     enabled: false

server:
  image:
    repository: ashu001/server
    tag: latest
    pullPolicy: IfNotPresent
  port: 8000
  replicaCount: 1
  mongodbUri: ""
  redisUrl: "redis://my-redis-master:6379"
  jwtSecretKey: "THIS_IS_A_JWT_SECRET_KEY"
  serverId: "{{ .Pod.Name }}"