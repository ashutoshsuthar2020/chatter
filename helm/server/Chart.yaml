apiVersion: v2
name: chat-server
description: Minimal Helm chart for chat server (backend)
version: 0.1.0
appVersion: "1.0.0"
type: application
dependencies:
  - name: nats
    version: "^0.19.17"
    repository: "https://nats-io.github.io/k8s/helm/charts/"
    condition: nats.enabled
  # - name: mongodb
  #   version: "^14.0.0"
  #   repository: "https://charts.bitnami.com/bitnami"
  #   condition: mongodb.enabled
